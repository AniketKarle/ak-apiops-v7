apis:
  - sgw-test-api

backends:
  - ignore

gateways:
  - openshift-gateway

diagnostics:
  - ignore

groups:
  - ignore

loggers:
  - ignore

namedValues:
  - ignore

products:
  - ignore

subscriptions:
  - ignore

tags:
  - ignore

policyFragments:
  - ignore

versionSets:
  - ignore

workspaces:
  - ignore
