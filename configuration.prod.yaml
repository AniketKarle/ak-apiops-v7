apimServiceName: azure-apim-prod01

namedValues:

policies:

loggers:

diagnostics:

gateways:
  - name: "openshift-gateway" # The name as it exists in your DEV artifacts
    newName: "self-hosted-gateway-2" # The name you want it to have in PROD
    
backends:

versionSets:

groups:

subscriptions:

products:

apis:
  - name: sgw-test-api

tags:

policyFragments:

workspaces:
