apimServiceName: azure-apim-prod01

namedValues:

policies:

loggers:

diagnostics:

gateways:
  - name: self-hosted-gateway-2
    apis:
      - name: test-sgw-api
    
backends:

versionSets:

groups:

subscriptions:

products:

apis:
  - name: test-sgw-api

tags:

policyFragments:

workspaces:
