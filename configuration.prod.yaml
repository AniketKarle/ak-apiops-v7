apimServiceName: azure-apim-prod01

namedValues:

policies:

loggers:

diagnostics:

gateways:
    
backends:

versionSets:

groups:

subscriptions:

products:

apis:
  - name: test-sgw-api

tags:

policyFragments:

workspaces:
