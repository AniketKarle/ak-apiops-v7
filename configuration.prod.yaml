apimServiceName: azure-apim-prod01

namedValues:

policies:

loggers:

diagnostics:

gateways:
    
backends:

versionSets:

groups:

subscriptions:

products:

apis:
  - name: ak-sgw-test-api01

tags:

policyFragments:

workspaces:
